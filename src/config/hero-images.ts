/**
 * Homepage hero background image(s).
 *
 * One entry => a static hero (today's behavior — zero slideshow JS runs).
 * Two or more entries => an auto-advancing, manually-swappable slideshow.
 *
 * The FIRST entry is always the LCP / priority image. Add, remove, or
 * reorder files here — no component change needed. Drop new files in
 * public/hero/ first.
 */
export const HERO_IMAGES: string[] = ["/hero/home-hero.webp"];
